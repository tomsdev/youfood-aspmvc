@model IEnumerable<tmf.web.ViewModels.StatViewModel>

@{
    ViewBag.Title = "Statistics by restaurant";
}

<h2><span class="titleContent">Statistics by restaurant</span></h2>
<div class="bodyContent">
<table>
    <tr>
        <th>
            Restaurant
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Benefice)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
         <td>
            @Html.DisplayFor(modelItem => item.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Benefice)€
        </td>
    </tr>
}

</table>
    </div>