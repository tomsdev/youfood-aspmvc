@model tmf.entities.ProductType

@{
    ViewBag.Title = "Delete a product type";
}

<h2><span class="titleContent">Delete a product type</span></h2>

<div class="bodyContent">
<h3><div class="deleteThis">Are you sure you want to delete this?</div></h3>
@*<fieldset>
    <legend>ProductType</legend>*@

    <div class="display-label">Description</div>
    <div class="display-field">@Model.Description</div>
    <br />
    <div class="display-label">Menus</div>
    <div class="display-field">@(Model.Menus == null ? "None" : Model.Menus.Count.ToString())</div><br />
@*</fieldset>*@
@using (Html.BeginForm()) {
    <p>
        <input class="submitInput" type="submit" value="Delete" />
        <div class="backToListLink">
        @Html.ActionLink("Back to List", "Index")
            </div>
    </p>
}


    </div>