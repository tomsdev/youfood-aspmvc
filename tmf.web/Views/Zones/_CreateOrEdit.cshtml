@model tmf.entities.Zone

@* This partial view defines form fields that will appear when creating and editing entities *@

<div class="editor-label">
    @Html.LabelFor(model => model.Name)
</div>
<div class="editor-field">
    @Html.EditorFor(model => model.Name)
    @Html.ValidationMessageFor(model => model.Name)
</div>

<div class="editor-label">
    Waiter
</div>
<div class="editor-field">
    @Html.DropDownListFor(model => model.WaiterId, ((IEnumerable<tmf.entities.Waiter>)ViewBag.PossibleWaiters).Select(option => new SelectListItem {
        Text = Html.DisplayTextFor(_ => option).ToString(), 
        Value = option.Id.ToString(),
        Selected = (Model != null) && (option.Id == Model.WaiterId)
    }), "Choose...")
    @Html.ValidationMessageFor(model => model.WaiterId)
</div>
